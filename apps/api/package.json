{
  "name": "@graph-ai-tutor/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "dev:watch": "tsx watch src/index.ts",
    "indexer:rebuild": "tsx src/indexer/cli.ts",
    "lint": "pnpm -w exec eslint apps/api --max-warnings 0",
    "typecheck": "pnpm -w exec tsc -p apps/api/tsconfig.json --noEmit",
    "test": "pnpm -w exec vitest run --root apps/api"
  },
  "dependencies": {
    "@graph-ai-tutor/db": "workspace:*",
    "@graph-ai-tutor/llm": "workspace:*",
    "@graph-ai-tutor/shared": "workspace:*",
    "chokidar": "^3.6.0",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "pg-mem": "^3.0.5",
    "tsx": "^4.7.2"
  }
}
