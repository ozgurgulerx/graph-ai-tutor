{
  "name": "@graph-ai-tutor/shared",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "pnpm -w exec eslint packages/shared --max-warnings 0",
    "typecheck": "pnpm -w exec tsc -p packages/shared/tsconfig.json --noEmit",
    "test": "pnpm -w exec vitest run --root packages/shared"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
